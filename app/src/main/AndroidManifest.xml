<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rominakehl.bildung_4_0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HolzSpiel" />
        <activity android:name=".WiButler" />
        <activity android:name=".PickByLight" />
        <activity android:name=".Einstieg_1" />
        <activity android:name=".Einstieg_2"></activity>
    </application>

</manifest>